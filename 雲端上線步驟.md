# 雲端上線步驟

> 標「👤 你做」的是瀏覽器帳號申請/按鈕操作，只能你自己來。
> 標「🤖 我做」的是程式碼/指令操作，你把需要的資訊貼給我，我直接處理。

---

## 第一步：GitHub（放程式碼的地方）

**👤 你做：**
1. 到 [github.com](https://github.com) 註冊帳號（免費）
2. 登入後右上角 `+` → `New repository`
3. Repository name 填 `noodle-shop-ordering`，選 **Private**（不公開），不要勾任何初始化選項，按 `Create repository`
4. 建好後頁面上會有一個網址，長得像 `https://github.com/你的帳號/noodle-shop-ordering.git`，複製起來給我

**🤖 我做：** 拿到網址後，我把 `麵店點餐系統` 資料夾初始化成git專案並推上去。

---

## 第二步：Turso（雲端資料庫，資料不會不見）

**👤 你做：**
1. 到 [turso.tech](https://turso.tech) 註冊（可以直接用GitHub帳號登入，比較快）
2. 登入後在網頁後台按 `Create Database`，資料庫名稱隨意（例如 `noodle-shop`），地區選離台灣近的（例如新加坡/東京，如果有選項）
3. 建好後找到兩個東西給我：
   - **Database URL**（長得像 `libsql://noodle-shop-xxx.turso.io`）
   - **Auth Token**（在資料庫頁面裡有個 `Create Token` 或 `Generate Token` 按鈕，按下去複製）

**🤖 我做：** 拿到這兩個值後，我在Render後台幫你設好環境變數（下一步會用到）。

---

## 第三步：Render（主機，讓網站對外開放）

**👤 你做：**
1. 到 [render.com](https://render.com) 註冊（一樣可以用GitHub帳號登入，登入後Render能直接看到你剛建的repo，比較好連）
2. 登入後按 `New` → `Web Service`
3. 選擇剛剛的 `noodle-shop-ordering` repo，授權Render存取
4. 設定畫面裡：
   - **Name**：隨意（例如 `noodle-shop`），這會變成你網址的一部分
   - **Root Directory**：留空或填 `.`（如果你是整個repo只有這一個專案的話）
   - **Build Command**：`npm install`
   - **Start Command**：`npm start`
   - **Instance Type**：先選 `Free`
5. 往下找到 `Environment Variables`，新增三筆：
   | Key | Value |
   |---|---|
   | `STAFF_PASSWORD` | 你自己想一組新密碼，不要用 admin123 |
   | `TURSO_DATABASE_URL` | 第二步拿到的 Database URL |
   | `TURSO_AUTH_TOKEN` | 第二步拿到的 Auth Token |
6. 按 `Create Web Service`，等它跑完部署（第一次大概3-5分鐘）
7. 部署完成後，頁面上方會有一個網址，長得像 `https://noodle-shop-xxx.onrender.com`，這就是你的正式網站

**完成後把那個網址給我**，我幫你確認客人點餐頁、員工後台是不是都正常運作。

---

## 之後要注意的事

- **Render免費方案會「睡著」**：超過15分鐘沒人訪問，網站會進入休眠，下一個人打開時要多等20-30秒才會醒來載入。如果尖峰時段（18:00-20:00）不想讓客人等，之後可以升級成付費方案（約US$7/月）避免休眠，先免費測試沒問題再考慮升級即可
- **員工後台密碼**：記得用你自己設的新密碼登入，不要用預設的admin123
- **正式網址**：等你確認一切正常後，我可以幫你把QR code貼紙的連結換成這個正式網址（不用再依賴自己電腦的區網IP）

---

## 給我的資訊清單（照順序準備好就能一次接完）

- [ ] GitHub repo網址
- [ ] Turso Database URL
- [ ] Turso Auth Token
- [ ] （Render部署完成後）正式網址，回來給我確認
